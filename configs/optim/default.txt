optim {
    seed = 0  # random seed
    learning_rate = 0.0001 # reduced learning rate (try to adapt with smaller batch size)
    batch_size = 64  # examples per batch
    max_iters = 400000  # max number of mini-batch steps to take
}
